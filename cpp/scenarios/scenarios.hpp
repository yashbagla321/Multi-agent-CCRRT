/**
 * @file scenarios.hpp
 * @brief Scenario types — geometry is defined in config/scenarios.json (runtime).
 *
 * @deprecated Built-in C++ scenario factories were removed. Edit config/scenarios.json
 *             or the inline "scenarios" block in ccrrt.json instead.
 */

#pragma once

#include "ccrrt/runtime_config.hpp"
